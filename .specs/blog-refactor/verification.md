# Verification: 重构验证方案

## 全局验证命令（每个 Phase 完成后必须执行）

```bash
pnpm type-check    # TypeScript 类型检查，零错误
pnpm build         # 生产构建，零错误
```

任一命令失败，该 Phase 视为未完成。

---

## Phase 1 验证

### 自动化

```bash
pnpm type-check && pnpm build
```

### 人工验证清单

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | Lenis 平滑滚动 | 打开 `/logic/#/knowledge` → 滚动到底部 → 点击左下角返回顶部按钮 | 页面平滑滚动到顶部（非瞬间跳转），显示 Lenis 惯性动画 |
| 2 | TOC 锚点滚动 | 知识库页 → 选中一篇有 h2/h3 标题的文章 → 点击右侧 TOC 某一项 | 页面平滑滚动到对应标题 |
| 3 | 页面过渡动画 | 在首页和知识库页之间反复切换 → 观察切换动画 | SVG 遮罩有渐变色（非透明/纯黑），动画流畅 |
| 4 | Agent Skills 分类 | 知识库页侧边栏 → 展开 "Agent Skills" | 显示 7 篇文章（Skills是什么、规范说明、快速创建Skill 等） |
| 5 | 文章 tags 显示 | 打开 `/logic/#/articles` → 查看动态加载的文章 | 文章卡片显示 tags：测试、Hello World |
| 6 | 文章详情 tags | 点击任一 MD 文章进入详情 | tags 正常显示在页面中 |
| 7 | 文章图片加载 | 知识库 → claude-code 分类 → "Claude Code 如何工作" | 文章中所有图片正常显示，点击可放大预览 |
| 8 | 首页 CTA 单次触发 | 首页 → 快速连续点击 "学习 AI" 按钮 3 次 | 只触发一次导航，过渡动画完整播放后进入知识库页 |

---

## Phase 2 验证

### 自动化

```bash
pnpm type-check        # 必须通过（新增的 .ts 文件无语法错误）
```

> 注意：Phase 2 只新增文件，不修改已有代码，所以 `pnpm build` 也应通过。

### 人工验证

- 无需人工验证（新文件尚未被引用，不影响运行时行为）

---

## Phase 3 验证

### 自动化

```bash
pnpm type-check && pnpm build
```

### 人工验证清单

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | 知识库数据加载 | 知识库页 → 遍历所有 5 个分类 | 所有分类和文章正常加载，无白屏或报错 |
| 2 | 文章列表页 | 打开 `/logic/#/articles` | 5 篇文章（2 MD + 3 硬编码）正常显示 |
| 3 | 文章详情页 | 点击任意文章进入详情 | 内容正常渲染 |
| 4 | 返回顶部按钮 | 打开文章详情页 → 滚动 → 点击返回顶部 | BackToTopButton 功能正常，环形进度条正确显示百分比，停止滚动后出现箭头 |
| 5 | 知识库返回顶部 | 知识库页 → 滚动 → 观察返回顶部按钮 | 与 T3.4 验证结果一致，行为完全相同 |
| 6 | BackToTopButton Teleport | ArticlesView 和 ArticleDetailView 中 BackToTopButton 正常 | Teleport 到 body 时按钮位置正确 |
| 7 | ArticleDetailView markdown | 打开文章详情页 | 内容渲染正常（标题、链接、代码块、列表），与重构前一致 |

---

## Phase 4 验证（重点）

### 自动化

```bash
pnpm type-check && pnpm build
```

### 核心验证：KnowledgeView 拆分后必须全部正常

> **每个 T4.x 完成后都要做对应范围的人工验证，不要等到全部改完再测。**

#### T4.4 完成后（桌面侧边栏）

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | 侧边栏显示 | 桌面宽度 (>948px) 打开知识库页 | 左侧固定侧边栏正常显示，包含 5 个分类 |
| 2 | 分类展开/折叠 | 依次点击每个分类标题 | 点击展开，再点击折叠，动画正常 |
| 3 | 文章选择 | 点击分类下的文章链接 | 主内容区切换到对应文章，URL hash 更新 |
| 4 | 当前文章高亮 | 选中一篇文章 | 侧边栏中该文章有 active 样式（左侧蓝色竖线、加粗） |
| 5 | 侧边栏折叠 | 折叠侧边栏 | 主内容区域自适应扩展 |
| 6 | 侧边栏滚动 | 侧边栏内容超出可视区时鼠标在侧边栏内滚轮 | 只滚动侧边栏，不滚动主内容 |

#### T4.5 完成后（桌面 TOC）

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | TOC 显示 | 桌面大屏 (>1200px) 选中一篇有标题的文章 | 右侧 TOC 栏显示文章所有 h1/h2/h3 标题 |
| 2 | TOC 层级 | 观察 TOC | h2 缩进 > h1，h3 缩进 > h2 |
| 3 | TOC 点击跳转 | 点击 TOC 中某个标题 | 页面 Lenis 平滑滚动到该标题 |
| 4 | TOC 高亮跟踪 | 缓慢滚动文章内容 | TOC 当前可见标题实时高亮（左侧蓝色圆点 + 加粗） |
| 5 | TOC 小屏隐藏 | 缩小窗口到 <1200px | TOC 消失 |
| 6 | TOC 空文章 | 切换到内容很短/无标题的文章 | TOC 为空时不显示 TOC 栏 |

#### T4.6 完成后（移动端面板）

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | 移动端顶栏 | 移动宽度 (<948px) 打开知识库页 | 顶部显示 "教程目录" 和 "页面导航" 两个按钮 |
| 2 | 教程目录面板 | 点击 "教程目录" | 左侧滑入面板，显示完整侧边栏，背景有半透明遮罩 |
| 3 | 面板内导航 | 在面板中展开分类 → 点击文章 | 面板关闭，主内容切换到对应文章 |
| 4 | 遮罩关闭 | 打开面板 → 点击遮罩区域 | 面板关闭 |
| 5 | 页面导航面板 | 点击 "页面导航" | 右侧滑入面板，显示 TOC |
| 6 | TOC 导航关闭 | 点击 TOC 项 | 面板关闭，页面滚动到目标位置 |
| 7 | 滚动锁定 | 打开任一面板 → 尝试滚动背景页面 | 背景页面不滚动 |
| 8 | 滚动恢复 | 关闭面板 | 背景页面恢复滚动 |
| 9 | 两边互斥 | 先打开教程目录 → 再点页面导航 | 教程目录关闭，页面导航打开（不同时显示） |

#### T4.7a 完成后（共享 Markdown 共存模式）

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | 无 console error | 打开知识库各分类文章，检查浏览器控制台 | 无 error、无 assert 失败 |
| 2 | 渲染正常 | 遍历 5 个分类各选 1-2 篇文章 | 所有文章内容正常显示 |

#### T4.7b 完成后（共享 Markdown 切换）

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | 标题渲染 | 查看文章中各级标题 | h1/h2/h3 样式正确、ID 正确（可作为锚点跳转） |
| 2 | 链接渲染 | 查看内链和外链 | 外链有 target="_blank" + ↗ 图标，内链正常 |
| 3 | 图片渲染 | 查看文章中图片 | 图片路径正确，可点击放大 |
| 4 | 图片预览 | 点击 markdown 图片 | ImagePreview 弹窗显示大图，ESC/点击遮罩关闭 |
| 5 | 代码块渲染 | 查看 ```代码块和 `行内代码` | 代码块有深色背景，行内代码有高亮 |
| 6 | 表格渲染 | 查看包含表格的文章 | 表格有圆角边框、斑马纹、小屏可横向滚动 |
| 7 | 引用块渲染 | 查看 blockquote | 左侧蓝色竖线 + 圆角背景 + 阴影 |
| 8 | 前后章导航 | 查看文章底部 | "上一章"/"下一章" 链接正确，点击正常切换 |

---

## Phase 5 验证

### 自动化

```bash
pnpm type-check && pnpm build
```

### 人工验证

| # | 验证场景 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 1 | 路由正常 | 遍历所有 5 个路由 | 每个路由正常加载，无报错 |
| 2 | AppHeader 页头 | 首页和知识库页切换 | 页头导航和菜单正常工作 |
| 3 | About 页面 | 打开 `/logic/#/about` | 页面正常渲染，无样式异常 |
| 4 | 暗色模式 | 切换暗色模式 | 所有页面正常工作 |
| 5 | 变量命名 | 检查 `src/data/knowledge.ts` | 变量名 `buildWithClaudeCodeArticles` 大小写统一 |
| 6 | Console 错误 | 遍历所有 5 个路由 | 浏览器控制台无 error、无 warning |

---

## Phase 6 验证

### 人工验证

逐条阅读 CLAUDE.md，对照项目实际结构验证：

| # | 验证项 | 验证方法 |
|---|--------|---------|
| 1 | 目录结构正确 | 检查 `src/` 下实际目录与 CLAUDE.md 描述一致 |
| 2 | 组件列表完整 | `src/components/` 下所有 .vue 文件都在 CLAUDE.md 中有对应说明 |
| 3 | Composable 列表完整 | `src/composables/` 下所有 .ts 文件有说明 |
| 4 | 工具模块说明准确 | `src/utils/` 下每个文件的职责描述与实际一致 |
| 5 | 所有文件路径可验证 | 文档中提及的每个文件路径确实存在 |
| 6 | 命令可执行 | `pnpm dev`、`pnpm build`、`pnpm lint`、`pnpm type-check` 均能正常运行 |

---

## 回归验证（全部 Phase 完成后）

```bash
pnpm type-check && pnpm build && pnpm lint
```

### 全量页面回归

| 路由 | 页面 | 检查项 |
|------|------|--------|
| `/` | 首页 | 预加载动画 → Hero 动画 → CTA 按钮 → 导航到知识库 |
| `/knowledge` | 知识库 | 侧边栏导航 → 文章内容 → TOC → 图片预览 → 前后章 → 返回顶部 → 暗色模式 |
| `/articles` | 文章列表 | 文章卡片 → tags → 点击进入详情 |
| `/articles/:id` | 文章详情 | 内容渲染 → BackToTopButton → 返回文章列表 |
| `/about` | 关于页面 | 内容渲染 → 暗色模式 |

### 响应式回归

| 宽度 | 检查项 |
|------|--------|
| < 768px | 移动端所有页面、移动端面板、汉堡菜单 |
| 768px ~ 947px | 平板布局、知识库单栏 |
| 948px ~ 1199px | 桌面小屏、知识库双栏 |
| ≥ 1200px | 桌面大屏、知识库三栏（含 TOC） |
